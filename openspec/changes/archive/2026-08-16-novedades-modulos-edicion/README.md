# README — archive package

Change packages archived **2026-08-16** from branch work on `feature/tiene-produccion-force`.

## Archive order (for readers)

1. `2026-08-16-novedades-tiene-produccion` — proxy + force-load + motivo/obs en cargas (`0017`)
2. `2026-08-16-novedades-modulos-edicion` — flag `produccion` en módulo (`0018`) + Param modals + skip check

Stable product spec: `openspec/specs/novedades/spec.md`.
